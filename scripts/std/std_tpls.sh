# This script must only be sourced!

# LCM
#if [ "${LCM_LIB}" == "" ] ; then
  export LCM_VER=1.3.1
  export LCM_VERSION=lcm-${LCM_VER}
  export LCM_LIB=${TPL_INSTALL_DIR}/common/${LCM_VERSION}/lib
#fi
